{
  "name": "snapconvert",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Free online file converter running entirely in the browser (ffmpeg.wasm) — static site, no server",
  "scripts": { "build": "node build.mjs", "dev": "node build.mjs && node scripts/serve.mjs", "serve": "node scripts/serve.mjs" },
  "engines": { "node": ">=20" }
}
